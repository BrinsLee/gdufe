package com.brin.util

import com.brin.gdufe.bean.Schedule
import java.util.ArrayList

class ScheduleUtil {

    /*private var mListTimeTable: MutableList<Schedule> = ArrayList()
    private var mCurrentWeek = -1

    fun setScheduleData(list: List<Schedule>) {
        this.mListTimeTable.clear()
        mCurrentWeek = FileUtil.SP_WEEK_NOT_SET
        for (schedule in list) {
            this.mListTimeTable.add(schedule)
            addTimeName(schedule.getName())
        }
        initView()
        invalidate()
    }*/
}